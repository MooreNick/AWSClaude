# requirements.txt - Python dependencies for the RAG tool Lambda layer
# Install into layer: pip install -r requirements.txt -t layers/dependencies/python/
# NOTE: boto3 is NOT included here - it is already built into the Lambda runtime

# PDF text extraction library
pypdf>=4.0.0

# Microsoft Word (.docx) document text extraction
python-docx>=1.1.0
